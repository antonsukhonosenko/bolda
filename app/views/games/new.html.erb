<h1>Create new game</h1>

Input a word of 5 to 8 letters

<% form_for(@game) do |f| %>
    <%= render "error_messages", :target => @game %>
    <%= f.text_field :letters, :value => '' %><br />
    <%= f.hidden_field :row_letters %>
    <%= f.submit %>
<% end %>
